<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:padding="16dp"
    android:gravity="center_horizontal"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <TextView
        android:text="ℹ️ Elementos de información"
        android:textStyle="bold" android:textSize="20sp"/>

    <TextView
        android:text="Presentan datos al usuario sin requerir entrada. Ejemplos: textos, imágenes y barras de progreso."
        android:layout_marginBottom="12dp"/>

    <ImageView
        android:id="@+id/img"
        android:src="@mipmap/ic_launcher_round"
        android:contentDescription="Logo"
        android:layout_width="96dp" android:layout_height="96dp"/>

    <ProgressBar
        android:id="@+id/progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:max="100" android:progress="0"/>

    <Button
        android:id="@+id/btn_progress"
        android:text="Simular carga"
        android:layout_marginTop="12dp"
        android:layout_width="wrap_content" android:layout_height="wrap_content"/>
</LinearLayout>
